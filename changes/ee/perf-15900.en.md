Improved memory efficiency when parsing MQTT packets.

Now, fields such as client ID, username, password, and topic are copied into new binaries instead of being slices from the raw packet.
This change helps reduce memory usage by allowing large packet buffers to be freed sooner.
