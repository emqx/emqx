Fixed a CoAP DTLS connection-mode regression where the gateway channel could be closed immediately on transport `sock_closed` after token issuance, causing the client/session to disappear unexpectedly.

Also added DTLS token takeover support: a reconnecting CoAP client using the same `clientid` and valid `token` can resume/take over the existing session.
