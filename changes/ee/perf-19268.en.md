Reduced the memory of each MQTT connection by about 80 bytes. Connections in the same zone that use the same MQTT protocol version now share one copy of the packet parser and serializer settings instead of each keeping its own.
