Improved redaction of sensitive data in logs, traces, and API responses.

Secrets are now consistently hidden in the affected places, including
authentication and authorization backend query traces, JWT signing key
material (carried inside `jose_jwk` records), HTTP connector request
headers, Prometheus configuration API responses, and authenticator
creation responses.
