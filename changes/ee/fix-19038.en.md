Fixed an issue where importing a namespaced configuration from an older EMQX version (6.2 or earlier) could fail due to missing limiter configuration (`subscribes`), which does not exist in such versions.
