Fixed plugin packages being loaded before they were fully validated. A package that fails validation is now rejected before any of its code is loaded, and a failure while loading a package rolls back the modules, applications and code path entries it had already introduced.
