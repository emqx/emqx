Implemented auto-generated password hashing for built-in database authentication.
Use auto-generated passwords by default for built-in database authentication in the hardened security profile.
Limit manual password hashing to stronger algorithms in the hardened security profile.
Preserve each user's hashing algorithm to allow changing the algorithm without invalidating existing credentials.
