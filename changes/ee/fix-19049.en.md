Published the offline Docker image tarballs for each platform in the release download directory. The files are named `emqx-enterprise-<version>-docker-amd64.tar.gz` and `emqx-enterprise-<version>-docker-arm64.tar.gz`.

Stopped publishing the Snowflake ODBC Docker images (tags with the `-sf` suffix). Existing `-sf` tags on Docker Hub stay available, but new releases do not get one.
