Introduced fine-grained, scope-based access control for API keys and dashboard login users, and added per-SSO-backend forced MFA.

API keys now carry an optional `scopes` field. When set, requests are authorized against a fixed catalog of management scopes (connections, publish, data integration, access control, gateways, monitoring, cluster operations, system, audit, license) in addition to the role check. The `publisher` API key role is constrained to the `publish` scope only.

Dashboard login users gained their own `scopes` field that layers on top of the existing role-based check. Four new login-only scopes (`user_management`, `mfa_management`, `sso_management`, `api_key_management`) cover dashboard-only endpoints. Three of these (`user_management`, `sso_management`, `api_key_management`) are administrator-only; `mfa_management` may be held by any role for self-exemption from forced MFA. API keys cannot hold any of the four login-only scopes.

Two public catalog endpoints expose the scope vocabulary for UI consumption: `GET /api_key_scopes` and `GET /user_scopes`. Both are accessible to any bearer-authenticated caller. The `scopes` field is also surfaced in `GET /users`, `POST /users`, and `PUT /users/:username` responses; when not explicitly set, the response projects the role-default scope list.

Added per-SSO-backend `force_mfa` enforcement. Each backend (LDAP, OIDC, SAML) accepts an independent `force_mfa` flag. New SSO logins from a backend with `force_mfa = true` require MFA setup before a JWT is issued; existing users can be exempted or required individually by an administrator via `POST` / `DELETE` on `/users/:username/mfa`, and that decision overrides the live backend policy until the administrator changes it.

Behavior changes that follow from the new scope model:

- The `dashboard.default_username` user is now a protected break-glass account. It cannot be deleted, demoted from administrator, or have its `scopes` field set; only its `description` may be changed. `DELETE /users/:username` on the default user is rejected even when other administrators exist. The previous last-administrator check still applies to other administrator accounts.
- Self-service on a user's own record now respects scopes. Only the dedicated change-password and MFA self endpoints still bypass scope checks; other operations such as `PUT /users/:self` are subject to the user's scopes. A viewer without `user_management` can still change their own password and manage their own MFA, but can no longer edit other profile fields on their own record without being granted the relevant scope.
- `PUT /users/:username` and `PUT /api_key/:name` validate role changes against the effective persisted scopes when the request body omits the `scopes` field. Demoting a user or changing an API key role is rejected if the persisted scopes are incompatible with the new role, ensuring role and scopes stay consistent even across partial updates.
- API key bootstrap files accept an optional fourth column for scopes (`key:secret:role:scopes`). Unknown or role-incompatible scope names are dropped with a warning rather than rejecting the whole file, so existing three-column bootstrap files remain loadable.
- SSO users on a `force_mfa = true` backend who disable their own MFA are required to set MFA up again on the next login. Only an administrator-initiated disable exempts a user from the live `force_mfa` policy; a self-initiated disable does not.
- The SAML SP metadata endpoint (`GET /sso/saml/metadata`) is now reachable without authentication, matching `/sso/saml/acs`. Identity providers fetching SP metadata, and administrators inspecting the endpoint from an authenticated dashboard session, both succeed.
