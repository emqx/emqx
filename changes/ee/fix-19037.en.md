CoAP Gateway credentials are no longer written to the connection debug logs.
The session token issued on `POST /mqtt/connection` and credentials sent with
the short query aliases (`t`, `p`) are redacted, while the rest of the packet
is logged as it was sent.
