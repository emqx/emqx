Fixed an issue where importing a namespaced configuration from an older EMQX version (6.1 or earlier) could fail due to missing limiter configuration (`delivery_bytes`, `delivery_messages`, `subscribes`), which does not exist in such versions.
