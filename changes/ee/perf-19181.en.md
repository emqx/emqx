Reduced the memory a connection uses when no message, byte or subscribe rate limit is configured for its zone and listener.

Such a connection no longer allocates its rate limiter state. A limit configured later still applies to connections that are already established.
