Non-administrator dashboard users and API keys now get a `403` response when they request the global configuration as `text/plain` from `GET /api/v5/configs`. Only administrators can export that document, which is the cleartext half of the `GET /configs` -> `PUT /configs` workflow. The redacted `application/json` variant of the same endpoint is unchanged and remains available to every authenticated user.
