Make sure that backup file operations for a namespace always stay within that namespace's own backup directory. Backup operations are not available for a namespace whose name cannot be used as a directory name, such as `.`, `..`, or a name containing a path separator.
