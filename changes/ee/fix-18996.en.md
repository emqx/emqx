Fixed a regression that prevented importing configurations containing valid legacy MQTT or Gateway listener names longer than 64 bytes.

Such names are now preserved during configuration imports and mixed-version cluster replication, while newly created listener names remain limited to 64 bytes.
