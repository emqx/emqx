Limit concurrent CoAP and LwM2M block-wise exchanges and retained payload bytes per session. Block1 uploads now receive 4.29 when the exchange limit is reached or 4.13 when the byte limit is reached. Temporarily blocked Observe notifications and LwM2M downlinks remain queued and resume when capacity becomes available.
