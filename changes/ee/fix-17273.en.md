Reduced startup log noise: the broker no longer emits `path_missing_from_scopes_map` warnings for built-in pre-login endpoints (`/login`, `/logout`, `/sso/running`, `/sso/login/:backend`, `/sso/token_exchange`) and scope catalog endpoints (`/user_scopes`, `/api_key_scopes`). These paths are now explicitly declared as public in each API module's scope map; runtime authorisation behaviour is unchanged.
