Previously, when checking its health status, Pulsar Connector would always use a 5 s timeout when doing an internal call.  Now, it uses the configured `resource_opts.health_check_timeout` (default: 10 s), which allows more fine tuning if necessary.
