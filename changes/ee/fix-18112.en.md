When `multi_tenancy.post_auth_tns_expression` is configured and evaluates to an empty value or fails to evaluate, a client whose pre-authentication `client_attrs.tns` is a reserved namespace name (`global`, `undefined`, `null`, `none`) is now denied with CONNACK reason code `not_authorized`, consistent with the handling of reserved namespace names when the expression evaluates to a non-empty value.
