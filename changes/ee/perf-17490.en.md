Migrated the broker's internal JSON encoding/decoding facade from the `jiffy` NIF library to OTP 27's native `json` module. The public API of `emqx_utils_json` is unchanged. The migration reduces NIF dependencies and avoids loading the jiffy port driver at startup for paths that use the broker's JSON facade.
