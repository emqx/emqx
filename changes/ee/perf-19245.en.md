Reduced broker memory use by about 110 bytes per MQTT client that subscribes over TCP, TLS or WebSocket, which is about 110 MB for one million such clients.

Improved the performance of the `emqx ctl subscriptions show <ClientId>` command. It no longer scans all subscriptions on the node to find those of one client.
