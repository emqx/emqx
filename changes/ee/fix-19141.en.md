Restored the `root` entry in the docker image's `/etc/passwd`, so `docker run --user root` works again. It was dropped in 6.3.1 when the image moved to a hardened base, and only the numeric form `--user 0:0` worked.
