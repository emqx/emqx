Added a new kind of listener (client exclusive) limiter: subscribe packet limiter.  It is disabled by default.  Once set to a finite rate, subscribe packets that exceed the configured rate will be replied with a Quota Exceeded reason code and not be processed.  Namespaces may specify their own, independent rate, and such value will overwrite the one from the listener.
