Fixed Kafka sources consuming records and committing their offsets during node startup, before MQTT clients could connect, so the records produced while the node was down were lost. Until startup completes, each Kafka source now reports `connecting` with the reason "Waiting for the node to be ready" and raises a resource alarm that clears once it connects; a subscriber that subscribes only after the source has consumed the backlog still misses it.
