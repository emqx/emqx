CI: `build_slim_packages / linux (amd64)` now reuses the `_build/` artifact produced by the upstream `compile (release)` job instead of recompiling from scratch, saving ~8m of duplicate compile work per run. The `arm64` slim job is unchanged because the upstream compile job is amd64-only.
