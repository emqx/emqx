In the hardened security profile, delayed messages from MQTT and gateway clients now store a restricted authorization context and are checked against current publish authorization rules when replayed. The legacy security profile retains the previous authorization context and delayed replay behavior.
