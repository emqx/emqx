Strengthen dashboard administrator password and API key secret hashing.
New credentials and rehashed legacy credentials use PBKDF2-HMAC-SHA256
with 600,000 iterations and a 16-byte random salt, replacing the previous
single-iteration SHA-256 with a 2-byte salt.
Existing stored hashes continue to work and are transparently upgraded
to the new format on the next successful authentication.
