Fixed an issue where installing and starting the sync request plugin on EMQX 6.x could fail to register its API endpoint because the plugin's API documentation contained literal descriptions rejected by strict i18n validation.

The Dashboard Swagger generator now supports an explicit plugin opt-in for literal API documentation. The plugin keeps its readable `schema/1` definition while retaining request validation, body translation, HTTP responses, and the `publish` API key scope.
