Fixed an issue where installing and starting the sync request plugin on EMQX 6.x could fail to register its API endpoint because the plugin's API documentation was not compatible with strict i18n validation.

The plugin API endpoint now starts without the `missing_i18n_ref` error. Its request validation, HTTP responses, and `publish` API key scope remain unchanged.
