Fixed repeated `badarg` errors in the log when a message was published while the schema validation, message transformation, or rule engine topic index was briefly unavailable, for example during broker startup or shutdown. Such a publish now proceeds as if no validation, transformation, or rule matched the topic.
