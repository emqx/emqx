Closed a privilege-escalation gap where API keys with the system scope, dashboard viewer accounts, and namespaced administrators could download stored backup archives whose contents include dashboard accounts (with password hashes / MFA state) and API key records. Downloading such archives now requires a global administrator. The stored-backup listing endpoint (`GET /data/files`) is also restricted to the global administrator.
