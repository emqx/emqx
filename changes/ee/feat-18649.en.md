Documented a previously unlogged hardened-profile behavior: in the `hardened` security profile, Dashboard login with the unchanged built-in default administrator credentials is rejected, and the default password must be changed before logging in. Both password login and the SCRAM login flow enforce this. The `legacy` security profile (the default) is unaffected and continues to allow login with the default credentials.
