Documented a previously unlogged hardened-profile behavior: in the `hardened` security profile, Dashboard login is rejected when the built-in default administrator password (`public`) has not been changed. The default password must be changed before logging in. The `legacy` security profile (the default) is unaffected and continues to allow login with the default credentials.
