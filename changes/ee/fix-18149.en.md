In the hardened security profile, delayed messages from MQTT and gateway clients now store a restricted authorization context and are checked against current publish authorization rules and bans when replayed. Message ingress now prepares messages before authorization and applies mountpoints consistently across MQTT and gateways. The legacy security profile retains the previous authorization context and delayed replay behavior.
