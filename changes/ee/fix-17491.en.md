Fixed password and secret leaks in gateway authentication APIs and ExProto debug logs. Gateway authentication API responses now redact secrets while preserving raw configuration, and ExProto authentication debug logs no longer print request passwords.
