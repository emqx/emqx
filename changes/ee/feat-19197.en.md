Added Traditional Chinese (`zh-TW`) for configuration and API descriptions.

`dashboard.i18n_lang` now accepts `zh-TW` alongside `en` and `zh`. Each language is independent: a description missing from `zh-TW` shows the English text, not the Simplified Chinese one.
