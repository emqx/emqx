Only the global administrator can now export the configuration as `text/plain` from `GET /api/v5/configs`. Other dashboard users and API keys, including viewers and namespaced users, get a `403` response. This export is not fully redacted and pairs with `PUT /api/v5/configs`. The redacted `application/json` variant of the same endpoint is unchanged.
