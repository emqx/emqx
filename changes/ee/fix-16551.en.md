Introduce new subscription API for streams. Now, a client can subscribe to a stream using `$stream` prefix: `$stream/<topic_filter>`. Start time can be specified using `start_from` user subscription property.
