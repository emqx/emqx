Fixed the REST API response for a malformed `Authorization` header. Such a request now gets a `401` response with the `AUTHORIZATION_HEADER_ERROR` code, the same as a request without the header. Previously, the request process exited, the client got an empty `400` response, and a crash report was written to the log.
