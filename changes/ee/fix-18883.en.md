Restricted the audit log to global users. The audit log records operations from every namespace, so only global administrators and global viewers can read it through `GET /api/v5/audit`. Dashboard users and API keys that belong to a namespace can no longer read it.
