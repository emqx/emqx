Fixed GreptimeDB actions to treat invalid integer, unsigned-integer, and Unicode point values as unrecoverable transformation errors. Malformed batches are now dropped instead of entering blocked retry processing and preventing subsequent messages from being sent.
