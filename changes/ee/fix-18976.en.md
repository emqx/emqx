Fixed GreptimeDB actions to treat invalid integer, unsigned-integer, and Unicode point values as unrecoverable transformation errors. In a malformed batch, only messages containing invalid points are dropped, while valid messages are still written instead of entering blocked retry processing.
