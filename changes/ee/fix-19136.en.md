Fixed an issue where a cluster link could drop the messages in flight when the connection to the peer cluster was lost.  Failures that mean the connection is gone or was never established (for example a connect timeout, a DNS resolution failure, or a transport error) are now treated as recoverable, so the affected messages are retried until the request expires instead of being acknowledged and counted as failed.
