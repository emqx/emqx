Fixed `emqx ctl conf load --namespace <ns> --merge`: the loaded configuration is now merged over the namespace's stored configuration. Before this fix it was merged over the global configuration, so a namespaced merge could copy global values into the namespace.
