Optimized the NATS gateway publish hot path to reduce per-message overhead in frame parsing, subject/topic handling, metrics updates, and ACK/message build steps.

This improves publish throughput in common NATS workloads while keeping `release-62` behavior and configuration semantics unchanged.
