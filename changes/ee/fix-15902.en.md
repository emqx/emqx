Upgrade MQTT client library to 1.13.8

This fixes auto reconnect of MQTT bridge when:

- Bridge peer broker does not respond PINGRESP.
- Bridge over TLS encounters early transport layer issues (after sent CONNECT but before receive CONNACK).
