Fixed data integration sources consuming messages during node shutdown after the rule engine had stopped.

A Kafka source, for example, could commit offsets for records that no rule processed. Sources now stop as soon as `GET /status` reports the node as not running, before the MQTT listeners close. Their configuration is kept, and they start again with the node.
