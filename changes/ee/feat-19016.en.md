Added the `POST /certs/global/name/:name/ca` API. It adds CA certificates to a managed certificate bundle and keeps the CA certificates the bundle already has. Certificates already in the bundle are skipped.

Uploading files to a managed certificate bundle no longer leaves a partly written file when a write fails. When writing any of the uploaded files fails, the files in the bundle stay unchanged.
