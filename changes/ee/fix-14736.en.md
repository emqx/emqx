Fixed an issue where importing a data backup via `POST /data/import` or `emqx ctl data import` would lead to the new config overwriting existing Schema Registry configurations instead of merging with it.
