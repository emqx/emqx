Fixed an issue where a Message Queue could stop delivering new messages from a storage shard after its consumer restarted, for example after a node restart or after all subscribers had been disconnected for a while. This happened when the queue had already read and acknowledged every message in the shard's previous storage generation.
