Stop creating audit log records for CLI commands that only print help.

Running a command such as `emqx ctl api_keys` without a subcommand prints the usage text.
Previously this also wrote an audit log record with an empty argument list, which looked like an
operation had been performed. Commands that do work are still audited, including read-only ones.
