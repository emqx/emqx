Decoupled the license session-count collector from the gateway application.

Gateway-protocol sessions are now reported to the license collector through a callback that the gateway app registers at start time. Deployments that disable the gateway app no longer trigger license-stats errors, and the license app no longer carries a hard dependency on the gateway app.
