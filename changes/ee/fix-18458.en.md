Upgrade `oidcc` to `3.2.3`.
Fixes dashboard SSO (OIDC) login timeouts (`INTERNAL_ERROR: exit,{timeout,{gen_server,call,[...]}}`) while the provider configuration worker is busy refreshing its cached configuration.
