`PUT /api/v5/api_key/:name` now only changes the fields carried by the request body. Previously a partial update (e.g. `{"enable": false}`) reset the key's expiry to `infinity`, so a key that was meant to expire could be turned into a non-expiring one without the caller asking for it. The stored expiry is now kept, while an explicitly supplied `expired_at` still changes it.
