For JWT authentication, the claims to verify now may be provided as a list of maps:
```
[
    {
        name = "claim_name"
        value = "${username}"
    },
    ...
]
```
Also improved the documentation for the `verify_claims` parameter.

