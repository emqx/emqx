# Used by "mix format"
[
  inputs: ["{mix,.formatter}.exs", "{config,lib,test}/**/*.{ex,exs}"],
  locals_without_parens: [
    start_type: 1,
    overlay: 1,
    mkdir: 1,
    copy: 2,
    template: 2
  ]
]
