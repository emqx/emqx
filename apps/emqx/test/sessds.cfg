%% -*- mode:erlang -*-
%%
%% This configuration takes effect when
%% `emqx_persistent_session_ds_SUITE' runs via `sessds.spec'. It
%% enables more thorough fuzz testing than we can afford in CI:
{fuzzer,
 [ {n_tests, 100}
 , {max_size, 100}
 , {command_multiplier, 1}
 ]}.
