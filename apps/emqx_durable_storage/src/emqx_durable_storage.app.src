%% -*- mode: erlang -*-
{application, emqx_durable_storage, [
    {description, "Message persistence and subscription replays for EMQX"},
    % strict semver, bump manually!
    {vsn, "0.2.0"},
    {modules, []},
    {registered, []},
    {applications, [kernel, stdlib, rocksdb, gproc, mria, ra, emqx_utils, emqx_bitswizzle]},
    {mod, {emqx_ds_app, []}},
    {env, []}
]}.
